# 📊 Contact Management (Frontend)

**Contact Management System (Frontend)** — сучасний інтерфейс для роботи з клієнтами, продажами та ремонтами.  
Реалізовано у вигляді десктопного застосунку з використанням HTML, CSS, JavaScript та інтеграцією з бекендом на .NET API.

---

## 🚀 Основні можливості

- **Dashboard** — аналітика з реальними даними з бази (прибуток, кількість клієнтів, продажів, ремонтів)
- **Clients** — додавання, редагування, видалення та перегляд клієнтів з пагінацією
- **Sales** — реєстрація продажів із автоматичним підрахунком суми
- **Repairs** — створення та облік ремонтів, підв’язка клієнтів
- **Синхронізація з API** — усі дані підтягуються з MySQL через .NET backend

---

## 🧩 Технології

- **HTML5 / CSS3 / JavaScript (Vanilla)**
- **Electron обгортка для десктоп застосунку (Node.js)**
- **Modular renderer structure** — кожна сторінка окремим файлом (`clients.html`, `sales.html`, `repairs.html`, `dashboard.html`)
- **REST API integration** через `fetch`
- **Responsive UI** з уніфікованою стилізацією (спільні компоненти, кнопки, таблиці)
- **Icons** — SVG-ікони в окремій папці `icons/`

---

## ⚙️ Запуск проекту

1. Відкрити папку `renderer` у VS Code або іншому редакторі  
2. Переконатись, що запущений **backend** (`Contact.API`)  
3. Відкрити `index.html` або потрібну сторінку (через Live Server або Electron shell)  
4. Для коректної роботи API має бути доступне за адресою: http://localhost:5101
